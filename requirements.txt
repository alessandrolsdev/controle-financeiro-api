# Versões fixadas e auditadas com pip-audit.
# Reexecute `pip-audit` após qualquer alteração deste arquivo.

# --- Framework Web ---
fastapi==0.141.1
# Starlette 1.x corrige: reconstrução de URL a partir de Host não validado
# (PYSEC-2026-161 / PYSEC-2026-248), limites ignorados em corpos urlencoded
# (PYSEC-2026-249) e despacho de métodos HTTP não padronizados em HTTPEndpoint
# (PYSEC-2026-2280).
starlette==1.6.0
uvicorn[standard]==0.38.0
gunicorn==23.0.0
# 0.0.20 tinha DoS quadrático e poluição de parâmetros no parser urlencoded —
# exatamente o formato usado pelo endpoint de login (PYSEC-2026-3036/3037).
python-multipart==0.0.32

# --- Validação e Configuração ---
pydantic==2.13.4
pydantic-settings==2.7.1
email-validator==2.2.0
python-dotenv==1.2.2

# --- Banco de Dados ---
SQLAlchemy==2.0.52
# psycopg 3 substitui o psycopg2-binary: é a linha mantida ativamente,
# com suporte a pipeline e melhor tratamento de tipos.
psycopg[binary]==3.2.9
alembic==1.16.4

# --- Segurança ---
# PyJWT substitui o python-jose, que está sem manutenção ativa e acumula
# vulnerabilidades de confusão de algoritmo (CVE-2024-33663) e de negação de
# serviço por "JWT bomb" (CVE-2024-33664).
# A 2.13.0 corrige ainda a falta de validação do header `crit` e o desvio da
# allowlist de algoritmos via PyJWK (PYSEC-2026-120 / PYSEC-2026-176/179).
PyJWT==2.13.0
# argon2-cffi é usado diretamente, sem o passlib no meio: o passlib está sem
# release desde 2020 e quebra em Python 3.13 por depender de `crypt`, removido
# da biblioteca padrão.
argon2-cffi==25.1.0

# --- Rate limiting distribuído (opcional, recomendado em produção) ---
redis==5.2.1
